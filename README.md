# latest

This is a very simple library that runs an http server which serves
the "latest" file from a directory.

What "latest" means is user-configurable. By default, it's the file
in the directory with the latest modification time.

> [!NOTE]
> Dotfiles are always ignored! This cannot be overridden.


